name: GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install Poetry and Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x
          with:
            python-version: 3.x
- name: Run isort
        run: |
          isort . --check --diff
          export ISORT_EXIT_CODE=$?
          if [ $ISORT_EXIT_CODE -ne 0 ]; then
            echo "isort failed with error code $ISORT_EXIT_CODE"
            exit 1
          fi
- name: Install Poetry
        uses: 'poetry/install-poetry@v1'
          echo "$HOME/.poetry/bin" >> $GITHUB_PATH
      - name: Install Poetry
        uses: 'poetry/install-poetry@v1'
          echo "$HOME/.poetry/bin" >> $GITHUB_PATH
      - name: Add Poetry to PATH
        run: |
          poetry run isort . --check --diff
          poetry run setup.py install

      - name: Set up Python and Poetry
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Setup Poetry

      - name: Setup Poetry
        uses: 'actions/setup-poetry@v2'
        with:
          python-version: 3.x

      # Add other steps as needed
